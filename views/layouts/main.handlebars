<!DOCTYPE html>
<html lang="en">
  <embed src="./assets/morning-sun-sakura-girl.mp3" loop="true" autostart="true" width="2" height="0" id="backgroundSong">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script>
    const ws = new WebSocket("ws://localhost:3002");
        ws.addEventListener("open", () =>{
          console.log('We are connected');
          ws.send('A new user has loaded the game!!');
        });
        ws.addEventListener('message', function (event) {
            console.log(event.data);
        });
    </script>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css" />
    <title>isGame:True</title>
</head>
<body>
   
    {{{ body }}}

</body>
</html>
{{! inline script to handle click of logout button }}
<script>
  const logout = async function() { const response = await
  fetch('/api/user/logout', { method: 'POST', headers: { 'Content-Type':
  'application/json' }, }); if (response.ok) { document.location.replace('/'); }
  else { alert('Failed to log out'); } };
  document.querySelector('#logout-link')?.addEventListener('click', logout);
</script>